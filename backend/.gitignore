node_modules/
.env
dist/
*.log

# Artefactos de probar local con SQLite (ver README).
prisma/*.db
prisma/*.db-journal
# prisma/migrations/ SÍ se comitea: es la migración real de Postgres,
# generada contra la base de Railway (ver DEPLOY.md).
