# Vacío/ausente en local (usa el proxy de vite.config.js hacia :4000).
# En producción (Vercel), pon la URL real del backend en Railway:
# VITE_API_URL="https://tu-backend.up.railway.app/api"
VITE_API_URL=
